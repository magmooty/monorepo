FROM golang:1.21.12

WORKDIR /app

COPY go.mod go.sum ./

RUN go mod download

CMD ["sh", "-c", "CGO_ENABLED=1 GOOS=linux GOARCH=arm64 go build -v -x -o target/lib/linux-arm64/libwhatsapp.a -buildmode=c-archive ./pkg/lib/lib.go"]
